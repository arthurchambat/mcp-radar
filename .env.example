# Optional. Raises GitHub API limits for enrichment and GitHub topic discovery.
GITHUB_TOKEN=

# Optional. Port for the local browser UI.
PORT=8787

# Optional. Registry endpoint override for testing or self-hosted registry mirrors.
MCP_RADAR_REGISTRY_URL=https://registry.modelcontextprotocol.io/v0/servers
