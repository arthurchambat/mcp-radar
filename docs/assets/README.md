# Screenshots

Add launch screenshots here before publishing the repository.

Recommended files:

- `mcp-radar-search.png`
- `mcp-radar-digest.png`
- `mcp-radar-install-recipes.png`

Recommended capture:

1. Run `npm run sync && npm run enrich:github && npm run ui`.
2. Open `http://127.0.0.1:8787`.
3. Search for `inspect postgres database safely`.
4. Capture the results view with scores and install recipes expanded.
5. Generate the weekly digest and capture the digest panel.
